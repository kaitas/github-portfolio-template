# AGENTS.md - AIと初めてポートフォリオを作る人へ

このファイルには2つの役割があります。

- 前半は、AIコーディングアシスタントを初めて使う人の説明です
- 後半は、AIがこのリポジトリを安全に編集するための指示です

## AIコーディングアシスタントとは

チャットで依頼すると、同じフォルダにある`index.html`などを読んで編集してくれるAIです。
普通のチャットAIと違い、実際のファイルを変更できます。

ただし、AIは間違えることがあります。変更後は必ず公開ページを自分の目で確認してください。

## 最初に送る文章

何から始めればよいか分からない場合は、次をそのままAIへ送ってください。

```text
AGENTS.mdを先に読んでください。
私はGitHubとHTMLの初心者です。
ポートフォリオに載せる内容を、一度に1問ずつ質問してください。
本名、学籍番号、住所、電話番号、学校のメールアドレスは聞かないでください。
回答がそろったらindex.htmlを編集し、何を変更したか簡単に説明してください。
```

まだ作品や自己紹介の案がない場合は、AIではなく`wizard.html`を使っても構いません。
ウィザードは選択肢から内容を組み立てられます。

## 上手な頼み方

AIには「何を」「どのように」「どのファイルで」を伝えると成功しやすくなります。

### 自己紹介を作る

```text
index.htmlの自己紹介を3文で書いてください。
活動名はaoです。
映像編集と3DCGを学んでいます。
得意なのは編集とライティングです。
将来はゲームの映像演出に関わりたいです。
本名や学校名は書かないでください。
```

### 作品を追加する

```text
index.htmlに作品を1つ追加してください。
タイトルは「夜の図書館」、制作年は2026年です。
画像はimages/work-02.pngです。
Blenderでモデリングとライティングを担当しました。
既存の作品と同じ形にし、スマートフォンでも画像が切れないようにしてください。
```

### 色を変える

```text
style.cssの配色を、暗い背景と明るい文字に変えてください。
アクセントは青紫にしてください。
文字が読みにくくならないようコントラストを確認してください。
HTMLの内容は変えないでください。
```

### YouTubeを載せる

```text
index.htmlにYouTubeへのリンクを追加してください。
URLはhttps://www.youtube.com/@exampleです。
新しいタブで安全に開くようにしてください。
```

### 間違いを直す

```text
公開ページでimages/work-02.pngが表示されません。
実際のファイル名とindex.htmlを確認して、原因を直してください。
他の内容は変えないでください。
```

## 依頼するときの注意

- APIキー、パスワード、学生証の写真をAIへ渡さない
- 「全部いい感じに」ではなく、変えたい場所を1つずつ伝える
- 残したい文章やデザインは「変えないでください」と伝える
- 分からない言葉が出たら「初心者向けに説明してください」と頼む
- 公開やpushを頼む前に、変更内容を説明してもらう

AIへ`push`を頼むと、変更がGitHubへ送られ、公開ページも更新されます。
まだ試している途中なら「pushはしないでください」と伝えてください。

---

# AIコーディングアシスタントへの指示

ここから先は、このリポジトリを編集するAIへのルールです。

## 利用者

利用者はデジタルコンテンツ系の大学生で、GitHub、HTML、CSS、JavaScriptを初めて使う可能性が高い。
専門用語を避け、必要な場合は直後に短く説明すること。質問は一度に1つにすること。

個人情報をポートフォリオへ載せるよう勧めないこと。本名、学籍番号、住所、電話番号、学校の
メールアドレスは不要。活動名、公開用SNS、作品に必要な情報を優先すること。

## このリポジトリの目的

作品をGitHub Pagesで公開する、ビルド不要の最小ポートフォリオテンプレート。

```text
index.html    公開されるポートフォリオ
style.css     公開ページの見た目
wizard.html   質問に答えてポートフォリオを作る画面
wizard.js     ウィザードの処理
images/       作品画像
README.md     初心者向けの公開手順
AGENTS.md     利用者向けAIガイドとAIへの指示
```

## 絶対に守ること

### ビルドを追加しない

`index.html`をそのまま配信する。npm、フレームワーク、トランスパイル、`package.json`、`src/`を
追加しない。失敗時に利用者自身が直せることを、コード上の整理より優先する。

### 小さく変更する

依頼された箇所だけ変更する。新しいファイルや抽象化を安易に追加しない。
既存の`index.html`と`style.css`だけで実現できる場合は、そこで完結させる。

### 自分で作りたい利用者をウィザードへ縛らない

ウィザードは「何を載せればよいか分からない人」の出発点であり、必須の実行環境ではない。
利用者が自分でHTMLを作りたい場合、公開に必要なのは`index.html`、必要なら`style.css`と`images/`だけ。
`wizard.html`と`wizard.js`は削除できる状態を維持する。

### HTMLを人が編集できる形に保つ

手書き版`index.html`では、作品は`<figure class="work">`をコピーして増やせる形を保つ。
書き換え箇所を示す次のコメントを消さない。

```html
<!-- ▼ ここから書き換えます -->
<!-- ▲ ここまで -->
```

テンプレート構文や、JavaScriptがないと本文を読めない構造へ変更しない。

### ウィザードはブラウザ内で完結させる

- 入力内容を外部APIへ送信しない
- GitHubトークンやAPIキーを要求しない
- 本人の明示操作なしに入力内容を永続保存しない
- JSON読込では形式を検証する
- HTML生成時は入力をエスケープし、URLはHTTPSまたは`images/`相対パスに限定する
- 同じ入力からは同じ内容を生成し、AI APIへ依存しない

### 権利と制作記録を残す

生成AIを使った旨の表示を、利用者の確認なく削除しない。
各作品のプロンプトや使用ツール、担当範囲を記録できる形を保つ。
第三者の著作物や人物写真を掲載する場合は、公開権限を確認するよう案内する。

## 作業後にすること

1. 変更したファイルと内容を短く説明する
2. 画像パスとファイル名の大文字・小文字を確認する
3. PCとスマートフォン幅で読めることを確認する
4. JavaScriptを変更した場合は構文とウィザードの主要操作を確認する
5. `git diff --check`を実行する
6. pushや公開は、利用者に頼まれた場合だけ行う

エラーや未確認事項がある場合は隠さず、初心者にも分かる言葉で伝えること。

## 公開の仕組み

`.github/workflows/pages.yml`が`main`への保存を検知し、ファイルをそのままGitHub Pagesへ公開する。
警告で公開を止めないのは、締切直前に利用者が公開できなくなることを防ぐため。

公開URL:

```text
https://<Owner名>.github.io/<リポジトリ名>/
```

Organization配下ではOwner名がOrganization名になる。
